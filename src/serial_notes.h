#ifndef PIANO_SERIAL_NOTES_H
#define PIANO_SERIAL_NOTES_H

#include <map>
#include <string>

#include "notes.h"

extern std::map<std::uint16_t, Note> KEY_NOTE_PAIRS;

#endif // !defined(PIANO_SERIAL_NOTES_H)